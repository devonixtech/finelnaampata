{
  "provider": "node",
  "phases": {
    "install": {
      "cmds": ["npm install"]
    },
    "build": {
      "cmds": ["npm run build:backend"]
    }
  },
  "deploy": {
    "startCommand": "npm run start:prod --prefix backend"
  }
}
